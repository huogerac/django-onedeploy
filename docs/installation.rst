============
Installation
============

At the command line::

    $ easy_install django-onedeploy

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-onedeploy
    $ pip install django-onedeploy
